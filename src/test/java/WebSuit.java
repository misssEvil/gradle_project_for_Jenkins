import io.qameta.allure.Story;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import org.openqa.selenium.By;
import runner.BaseTest;

/*public class WebSuit extends BaseTest {

    @Story("Open main page")
    @Test
        public void openRedRoverWebsite(){
            getDriver().get("https://redrover.school");
            Assertions.assertEquals("RedRover | Non-commercial it-school", getDriver().getTitle());
        }

    @Story("Open main page")
    @Test
        public void headerIsPresent(){
            getDriver().get("https://redrover.school");
            Assertions.assertTrue(getDriver().findElement(By.xpath("//a[@href = '/#training']")).isDisplayed());
        }


    @Story("Open teachers page")
    @Test
    public void openTeachersPage(){
        getDriver().get("https://redrover.school");
        getDriver().findElement(By.xpath("//a[@href = '/en/teachers']")).click();
        Assertions.assertEquals(getDriver().getTitle(), "RedRover | Teachers");
    }
}*/
